enum ProductRoutesEnum {
  FETCH_PRODUCT_FILE = '/fetch',
  GET_PRODUCT = '/:code',
  GET_PRODUCTS = '/',
  DELETE_PRODUCT = '/:code',
  UPDATE_PRODUCT = '/:code',
}

export default ProductRoutesEnum
